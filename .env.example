VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY

# RevenueCat Web Billing — public key from Apps & providers → Web Billing app.
VITE_REVENUECAT_API_KEY=rcb_web_public_...
# Must match entitlement identifier in RevenueCat dashboard.
VITE_REVENUECAT_ENTITLEMENT_ID=premium

# Used only on the dev / preview server (never bundled into the client).
ANTHROPIC_API_KEY=sk-ant-api03-...
